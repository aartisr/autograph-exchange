# `@aartisr/autograph-contract`

Shared API contract for Autograph Exchange.

This package exports:

- route constants
- shared request and response shapes
- domain types used by both hosts and the reusable feature package

Use this package when a host wants to stay aligned with the standalone app's API contract without copying string paths or request types.
